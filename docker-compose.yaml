version: "3.8"

services:

  static_app:
    build: ./static_web/
    labels:
      - "traefik.http.routers.static_web.rule=Host(`localhost`)"

  todo_api:
    build: ./todo_api/
    labels:
      - "traefik.http.routers.todo_api.rule=Host(`localhost`) && PathPrefix(`/api`)"
      - "traefik.http.services.todo_api.loadbalancer.server.port=7070"
      - "traefik.http.middlewares.todo_api_stripprefix.stripprefix.prefixes=/api"
      - "traefik.http.routers.todo_api.middlewares=todo_api_stripprefix"

  reverse_proxy:
    # The official v2 Traefik docker image
    image: traefik:v2.10
    # Enables the web UI and tells Traefik to listen to docker
    command: --api.insecure=true --providers.docker
    ports:
      # The HTTP port
      - "80:80"
      # The Web UI (enabled by --api.insecure=true)
      - "8080:8080"
    volumes:
      # So that Traefik can listen to the Docker events
      - /var/run/docker.sock:/var/run/docker.sock